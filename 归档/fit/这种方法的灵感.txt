不同模式记录的基像素不同。不过这样需要在判断哪种模式好时编解码不同的基像素 代价有点大

增加两点间渐变模式  能一次性计算多个点  但是大范围内很可能不适用 要先加入分割

根据最终选用的不同预测方式,确定Huffman编码时的读顺序,比如按预测方向去读 这样比较有可能把0连起来

确定关键像素的时候, 不固定为某一像素, 而是取5x5中心3x3或者中心+上下左右像素的均值. 类似滤波

就观察像素值来说, 加权很有必要 尽快写出来

一个附带的小作用: base像素作缩略图, 点进去再进行解压